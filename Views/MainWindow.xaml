<Window x:Class="FinanceiroObserver.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:vm="clr-namespace:FinanceiroObserver.App.ViewModels"
        Title="Monitor de Mercado Pro" Height="650" Width="900"
        Background="#F5F5F7">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,15">
            <TextBlock Text="{Binding PrecoAtual}" FontSize="32" FontWeight="Bold" 
                       Foreground="{Binding CorTendencia}" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="15" Padding="15" Margin="0,0,0,15">
            <lvc:CartesianChart Series="{Binding SeriesCollection}" Hoverable="False" DataTooltip="{x:Null}">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis ShowLabels="False"></lvc:Axis>
                </lvc:CartesianChart.AxisX>
            </lvc:CartesianChart>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="15" Padding="10">
            <ListBox ItemsSource="{Binding Historico}" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:HistoricoItem}">
                        <Border Background="{Binding Cor}" Opacity="0.9" CornerRadius="8" Margin="2" Padding="10">
                            <TextBlock Text="{Binding Texto}" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>